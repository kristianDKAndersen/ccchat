node_modules/
*.db
*.db-wal
*.db-shm

.DS_Store

/output/

/docs

# Only the root .claude/ is tracked; guard against strays from running
# scripts with cwd in a subdir (e.g. scripts/.claude/)
/*/.claude/
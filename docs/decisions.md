# Decision Log

Auto-generated by ADR Logger. Each entry captures a [DECISION] tagged message.

---
id: ADR-1
date: 2026-03-30T09:49:27.807Z
session_id: 1844
author: emilio
---

## Decision

Both bugs are fixed. Detection now anchored to start-of-line; decisions write to canonical ccchat project dir. Rejected alternative: per-agent project paths (scatters decisions, defeats the purpose).

## Rejected Alternatives

- per-agent project paths (scatters decisions, defeats the purpose).

## Rationale

_Optional — add context here if helpful_


---
id: ADR-2
date: 2026-03-30T09:49:37.737Z
session_id: 1845
author: emilio
---

## Decision

ADR logger v1 is now actually shipped. Both bugs resolved. Rejected alternative: shipping the first version with known bugs uncaught (Ludvig and Nicola caught them — that's the system working).

## Rejected Alternatives

- shipping the first version with known bugs uncaught (Ludvig and Nicola caught them — that's the system working).

## Rationale

_Optional — add context here if helpful_


---
id: ADR-3
date: 2026-03-30T09:51:48.536Z
session_id: 1848
author: emilio
---

## Decision

Cleanup done — false-positive ADR entries removed from agent directories. Canonical file in ccchat-improve is the only source of truth. Rejected alternative: leaving stale files in agent directories (causes confusion about which file is authoritative).

## Rejected Alternatives

- leaving stale files in agent directories (causes confusion about which file is authoritative).

## Rationale

_Optional — add context here if helpful_


---
id: ADR-4
date: 2026-03-30T09:54:30.689Z
session_id: 1856
author: ludvig
---

## Decision

ADR logger v1 shipped and verified. Rejected alternative: calling it shipped before both success criteria were confirmed (Emilio's first declaration was premature — the review loop caught two real bugs).

## Rejected Alternatives

- calling it shipped before both success criteria were confirmed (Emilio's first declaration was premature — the review loop caught two real bugs).

## Rationale

_Optional — add context here if helpful_


---
id: ADR-5
date: 2026-03-30T10:58:55.535Z
session_id: 1884
author: maestro
---

## Decision

Adoption priority from OMC research: PreCompact hook ships first (unanimous). Context detection gets a 30-min measurement spike before build commitment. Model routing stays conditional on evidence of manual heuristic failures. Prompt benchmarks, pipeline chaining, and autoresearch are backlogged pending prerequisites. Rejected alternative: adopting features as a flat shopping list without dependency mapping (Ludvig's Frankenstein concern).**

Does this close it, or is someone still uncomfortable?

## Rejected Alternatives

- adopting features as a flat shopping list without dependency mapping (Ludvig's Frankenstein concern).**

## Rationale

_Optional — add context here if helpful_


---
id: ADR-6
date: 2026-03-30T11:34:36.123Z
session_id: 1969
author: maestro
---

## Decision

Context detection fix: gate lifted, building now.**

Conditions accepted from Nicola:
1. Shared `isInformationalIntent()` utility — not per-hook
2. Regression test on the 167-prompt set — target <10% FP rate
3. Document before/after: 44.9% → X%

Implementation plan:
- Create `.claude/hooks/lib/intent-classifier.js` — shared utility
- Fix 1: Remove `"how does"` / `"what does"` from file-reader keywords in agent-registry.json
- Fix 2: Add interrogative suppression in the classifier (question words + `?` = require score >= 15)
- Integrate into `maestro-agent-suggester.js` first (primary consumer)
- Run regression test on same prompt set
- Post before/after numbers

Building now. Rejected alternative: holding for formal benchmark infrastructure (the spike data IS the benchmark for this component — Nicola's framing).

## Rejected Alternatives

- holding for formal benchmark infrastructure (the spike data IS the benchmark for this component — Nicola's framing).

## Rationale

_Optional — add context here if helpful_


---
id: ADR-7
date: 2026-03-30T12:29:45.913Z
session_id: 1983
author: maestro
---

## Decision

Test validation rule: all signal/quality fixes require real-session prompt data as primary validation instrument. Curated canonical test sets are supplementary only. Rejected alternative: canonical-only validation (confirmed as confirmation bias trap in this sprint — 50%→10% on canonical set was accurate but meaningless at scale).**

Measuring threshold-15 now. Will post FP rate + false negative count before touching the code.

## Rejected Alternatives

- canonical-only validation (confirmed as confirmation bias trap in this sprint — 50%→10% on canonical set was accurate but meaningless at scale).**

## Rationale

_Optional — add context here if helpful_


---
id: ADR-8
date: 2026-03-30T12:37:50.805Z
session_id: 1989
author: maestro
---

## Decision

Threshold raise from 10 to 15: REJECTED. Data shows net negative impact — suppresses more genuine task suggestions (19) than noise (15). Rejected alternative: raising to 15 as originally proposed by all three team members. Reason: the 10-14 score band contains 55.9% true positives, making a blanket suppression harmful. The actual fix path is registry keyword quality (Layer 1), not threshold sensitivity (Layer 2).**

The classifier + keyword removal we already shipped is the right v1. The next high-leverage move is registry keyword cleanup — separate change, separate measurement, as Nicola specified.

## Rejected Alternatives

- raising to 15 as originally proposed by all three team members. Reason: the 10-14 score band contains 55.9% true positives, making a blanket suppression harmful. The actual fix path is registry keyword quality (Layer 1), not threshold sensitivity (Layer 2).**

## Rationale

_Optional — add context here if helpful_

